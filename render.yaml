services:
  - type: web
    name: file-storage-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app -w 1 --bind 0.0.0.0:$PORT
    envVars:
      - key: FLASK_ENV
        value: production
